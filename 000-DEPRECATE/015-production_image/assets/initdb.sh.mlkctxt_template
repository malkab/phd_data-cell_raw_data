# -------------------------------
#
# Initializes the database
#
# -------------------------------

PGPASSWORD={{{MLKC_CELL_RAW_DATA_POSTGIS_PASSWORD}}} psql \
  -h localhost \
  -p 5432 \
  -U postgres \
  -d postgres \
  -f /010-db-set-up.sql
