version: '3.5'

networks:
  phd_data:
    external: false
    name: {{{MLKC_PHD_DATA_APP}}}_production_test

volumes:
  phd_data_postgis:
    external: false
    name: {{{MLKC_PHD_DATA_APP}}}_postgis_production_test

services:
  postgis:
    image: {{{MLKC_PHD_DATA_PRODUCTION_IMAGE}}}:latest
    container_name: {{{MLKC_PHD_DATA_APP}}}_postgis_production_test

    environment:
      - PASSWORD={{{MLKC_PHD_DATA_POSTGIS_PASSWORD}}}

    networks:
      - phd_data

    ports:
      - "{{{MLKC_PHD_DATA_PG_EXTERNAL_PORT}}}:5432"

    volumes:
      - phd_data_postgis:/data
