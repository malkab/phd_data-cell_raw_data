version: '3.5'

networks:
  cell:
    external: true

volumes:
  cell_raw_data_postgis:
    external: false
    name: {{{MLKC_CELL_RAW_DATA_APP}}}_postgis

services:
  cell_raw_data:
    image: {{{MLKC_CELL_RAW_DATA_PRODUCTION_IMAGE_NAME}}}:latest

    environment:
      - PASSWORD={{{MLKC_CELL_RAW_DATA_POSTGIS_PASSWORD}}}

    networks:
      - cell

    ports:
      - "{{{MLKC_CELL_RAW_DATA_PG_EXTERNAL_PORT}}}:5432"

    volumes:
      - cell_raw_data_postgis:/data
